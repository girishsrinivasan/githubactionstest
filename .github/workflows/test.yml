name: Testing

on: 
 push:
   branches: ["*"]

jobs:
  test:
    name: 'Testing'
    runs-on:  ubuntu-latest 
    steps:
      - name: 'Run Tests'
        run: |
          echo "Test have run"

    