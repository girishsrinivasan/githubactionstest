on:
  workflow_call:
    
jobs:
  new_release:
    name: 'Create Release'
    runs-on:  ubuntu-latest 
    steps:
      - name: 'Create a new github release'
        run: |
          echo "Release created"